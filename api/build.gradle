

ext {
    outputSpec = project.file("$buildDir/api.json")
}


task compileSpec(type: org.activityinfo.swagger.CompileSpecTask) {
    yamlFile = project.file("src/main/spec/forms.yaml")
    outputFile = outputSpec
}